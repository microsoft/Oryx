'use strict';

import React from 'react'; // The React variable is used once the JSX is transformed into pure JS
import ReactDOM from 'react-dom';

import BrowseContainer from '../containers/browse-container';

ReactDOM.render(<BrowseContainer />, document.getElementById('content'));
