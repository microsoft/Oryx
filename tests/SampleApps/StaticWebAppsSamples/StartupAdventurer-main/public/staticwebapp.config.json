{
  "routes": [
    {
      "route": "/logged-in",
      "rewrite": "/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/js/*", "/static/*", "/api/*"]
  }
}
