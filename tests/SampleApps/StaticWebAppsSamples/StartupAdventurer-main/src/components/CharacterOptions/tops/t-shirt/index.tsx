import colors, { defaultColor } from "./colors";
import thumb from "./thumb";
import layer from "./layer";

const tshirt = { colors, defaultColor, name: "t-shirt", thumb, layer };
export default tshirt;
