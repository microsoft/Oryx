apiVersion: v1
kind: PersistentVolume
metadata:
  name: {0}
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - mfsymlinks
  azureFile:
    secretName: storage-secret
    shareName: {1}