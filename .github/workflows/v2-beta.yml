on:
  push:
    branches:
      - master
      - actions/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Build Python Django app using Oryx
      uses: Azure/appservice-build@v2-beta
      with:
        source-directory: tests/SampleApps/python/django-app
    
    - name: Build Python Django app using Oryx and explicit version
      uses: Azure/appservice-build@v2-beta
      with:
        source-directory: tests/SampleApps/python/django-app
        platform: python
        platform-version: 3.7

    - name: Build NodeJS webfrontend app using Oryx
      uses: Azure/appservice-build@v2-beta
      with:
        source-directory: tests/SampleApps/nodejs/webfrontend

