FROM ruby-build-prereqs
WORKDIR /usr/src/ruby
ARG VERSION_TO_BUILD
ARG GEM_VERSION
ARG RUBY_SHA256
RUN RUBY_VERSION=$VERSION_TO_BUILD \
    GEM_VERSION=$GEM_VERSION \
    RUBY_SHA256=$RUBY_SHA256 \
    /tmp/build.sh